{% extends 'template.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-9 mt-4">
                <h1>Edit Desktop</h1>

                {{ include('desktop/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_desktop_index') }}" class="btn btn-secondary m-4">back to list</a>

                {{ include('desktop/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
